exports.transferStablecoin = async (toWallet, amountUSDC) => {
  // Placeholder: integrate ethers.js or 0G SDK in production
  const rand = Math.random().toString(16).slice(2)
  return `0x${rand.padEnd(64, '0')}`
}